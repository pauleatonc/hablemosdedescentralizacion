# -----------------------------
# PostgreSQL configuration file
# -----------------------------

# ------------------------------------------------------------------------------
# LOGGING
# ------------------------------------------------------------------------------

# Establecer el destino de registro a stderr.
log_destination = 'stderr'

# Registrar todas las declaraciones SQL ejecutadas.
log_statement = 'all'

# Registrar cada conexión establecida y desconexión.
log_connections = on
log_disconnections = on

# Registrar la duración de cada comando SQL.
log_duration = on

# Desactiva el collector. Ya que los logs irán directo a stderr, no es necesario activar el collector.
logging_collector = off

